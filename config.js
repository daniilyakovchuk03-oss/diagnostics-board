/* ═══════════════════════════════════════════════════════════════
   НАСТРОЙКИ ДОСКИ

   Пока здесь стоят нули — доска запустится, но будет пустой.
   Откройте адрес-сервера/setup, там будут все нужные цифры,
   пришлите скриншот, и мы заполним этот файл вместе.
   ═══════════════════════════════════════════════════════════════ */

module.exports = {

  // Часовой пояс, в котором показываем время встреч
  TIMEZONE: 'Asia/Almaty',

  // Как часто перезапрашивать данные у амо, секунды
  REFRESH_SEC: 60,

  // ID воронки, в которой живут диагностики.
  // 0 — брать сделки из всех воронок (медленнее, но работает).
  PIPELINE_ID: 0,

  // ID поля «Дата и Время диагностики». Без него доска будет пустой.
  DATE_FIELD_ID: 0,

  // ID поля «Имя и фамилия». 0 — брать название сделки.
  NAME_FIELD_ID: 0,

  // ID поля «Преподаватель». 0 — определять колонку по ответственному за сделку.
  TEACHER_FIELD_ID: 0,

  // Колонки на доске.
  // amo_user_id — ID менеджера в амо (для распределения по ответственному)
  // teacher — как этот человек записан в поле «Преподаватель» (если оно используется)
  MANAGERS: [
    { id: 'tair',    name: 'Таир',   amo_user_id: 0, teacher: 'Таир' },
    { id: 'dulat',   name: 'Дулат',  amo_user_id: 0, teacher: 'Дулат' },
    { id: 'sanzhar', name: 'Санжар', amo_user_id: 0, teacher: 'Санжар' },
    { id: 'dancho',  name: 'Данчо',  amo_user_id: 0, teacher: 'Данчо' },
  ],

  // Что значит каждый этап воронки на доске.
  // came — зелёная галочка, no — красный крестик,
  // moved — перенос, wait — ожидает (значение по умолчанию).
  // Пример: { 12345678: 'no', 12345679: 'came' }
  STATUS_MAP: {},

  // Показывать ли колонку мастер-классов слева.
  // Включим, когда в амо появится поле с временем МК.
  MK_ENABLED: false,
};
